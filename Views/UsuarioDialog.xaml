<Window x:Class="InventarioAppDesktop.Views.UsuarioDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{Binding Titulo}" Height="420" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <md:Card Padding="32,28" Margin="16">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Image Source="pack://application:,,,/Resources/InventarioAppLogo.png"
                       Height="32" VerticalAlignment="Center" Margin="0,0,12,0"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
                <TextBlock x:Name="LblTitulo" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <TextBox x:Name="TxtNombre" md:HintAssist.Hint="Nombre de usuario"
                     md:TextFieldAssist.HasOutlinedTextField="True"
                     Margin="0,0,0,16"/>

            <TextBlock x:Name="LblPassword" Text="Contraseña" FontSize="13" Margin="0,0,0,4"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <PasswordBox x:Name="TxtPassword"
                         Padding="12,10" FontSize="14" Margin="0,0,0,16"
                         BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1"/>

            <TextBlock Text="Rol" FontSize="13" Margin="0,0,0,4"
                       Foreground="{DynamicResource MaterialDesignBody}"/>
            <ComboBox x:Name="CmbRol" Padding="10,8" FontSize="14" SelectedIndex="1"
                      Margin="0,0,0,20">
                <ComboBoxItem Content="admin"/>
                <ComboBoxItem Content="usuario"/>
            </ComboBox>

            <TextBlock x:Name="LblError" Foreground="#DC3545" FontSize="12" Margin="0,0,0,16"
                       TextWrapping="Wrap"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Style="{StaticResource MaterialDesignFlatButton}"
                        Click="BtnCancelar_Click" Margin="0,0,8,0"/>
                <Button Content="GUARDAR" Click="BtnGuardar_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="{StaticResource InventarioPrimary}" Foreground="White"
                        Padding="20,8"/>
            </StackPanel>
        </StackPanel>
    </md:Card>
</Window>
